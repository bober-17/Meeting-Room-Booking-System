-- Тестовые данные для ручного тестирования.
-- Запуск: make seed
-- Безопасно запускать повторно — все вставки идемпотентны (ON CONFLICT DO NOTHING).

-- ─────────────────────────────────────────────────────────────────────────────
-- Пользователи
-- admin и user создаются автоматически миграцией 0006 при старте.
-- Здесь они включены для полноты; повторная вставка ничего не изменит.
-- ─────────────────────────────────────────────────────────────────────────────
INSERT INTO users (id, email, role) VALUES
    ('00000000-0000-0000-0000-000000000001', 'admin@test.com', 'admin'),
    ('00000000-0000-0000-0000-000000000002', 'user@test.com',  'user')
ON CONFLICT (id) DO NOTHING;

-- ─────────────────────────────────────────────────────────────────────────────
-- Переговорки
-- ─────────────────────────────────────────────────────────────────────────────
INSERT INTO rooms (id, name, description, capacity) VALUES
    ('10000000-0000-0000-0000-000000000001', 'Переговорная 1', 'Стандартная переговорка', 12),
    ('10000000-0000-0000-0000-000000000002', 'Переговорная 2', 'Малая переговорка',        4),
    ('10000000-0000-0000-0000-000000000003', 'Переговорная 3', 'Без расписания',            8)
ON CONFLICT (id) DO NOTHING;

-- ─────────────────────────────────────────────────────────────────────────────
-- Расписания
--
-- Переговорная 1: пн–пт, 09:00–18:00 → 18 слотов в рабочий день.
--   Запрос на выходной день (сб, вс) вернёт пустой список.
--
-- Переговорная 2: каждый день, 08:00–20:00 → 24 слота в любой день.
--   Используйте её, если сегодня выходной.
--
-- Переговорная 3: расписание отсутствует → GET /rooms/{id}/slots/list
--   всегда возвращает пустой список независимо от даты.
-- ─────────────────────────────────────────────────────────────────────────────
INSERT INTO schedules (room_id, days_of_week, start_time, end_time) VALUES
    ('10000000-0000-0000-0000-000000000001', '{1,2,3,4,5}',     '09:00', '18:00'),
    ('10000000-0000-0000-0000-000000000002', '{1,2,3,4,5,6,7}', '08:00', '20:00')
ON CONFLICT (room_id) DO NOTHING;
